---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: "8"

inputs:
- name: attendee-service-source

outputs:
- name: build-artifacts

run:
  path: attendee-service-source/ci/tasks/package.sh

